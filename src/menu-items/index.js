import dashboard from './dashboard';
import members from './members';
import reports from './reports';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [members, reports]
};

export default menuItems;
